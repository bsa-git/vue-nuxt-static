{
  "name": "vue-nuxtjs-starter",
  "version": "1.0.0",
  "description": "Nuxt.js project",
  "author": "bsa-git <bsa2657@yandex.ru>",
  "private": true,
  "engines": {
    "node": "6.3.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.23.0",
    "nuxt": "latest"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
