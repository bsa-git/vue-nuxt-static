<template>
    <div class="footer-well">
        <div class="container">
            <div class="well">
                <div class="row">
                    <div class="col-md-8">
                        <h2 v-html="services.purchase.business.title"></h2>
                        <p v-html="services.purchase.business.summary"></p>
                    </div>
                    <div class="col-md-4">
                        <div class="button-side text-right">
                            <a class="btn btn-default"
                               href="/services"><i class="fa fa-gift"></i> See Features</a>
                            <a class="btn btn-primary"
                               :href="services.purchase.url"><i class="fa fa-shopping-cart"></i> Purchase It Now</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</template>

<script>
import { mapGetters } from 'vuex'

export default {
    created: function () {
        try {
            //--- Personal data ---//
            this.$store.dispatch('receiveServicesPurchase');
        } catch (error) {
            this.$store.commit('SET_ERROR', error);
            this.$router.replace('/error')
        }
    },
    computed: mapGetters({
        services: 'getServices',
    }),
}
</script>